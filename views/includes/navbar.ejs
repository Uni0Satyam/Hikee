<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><i class="fa-regular fa-compass"></i> Hikee</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>
      <div class="ms-auto d-flex search-container">
        <div class="container-fluid p-0">
          <form class="d-flex">
            <input class="form-control me-2 search-bar" role="search" type="text" placeholder="Search listings here"
              aria-label="Search" name="key"/>
              <select name="q" id="category" class="form-control ">
                <option value="country" selected>Country</option>
                <option value="city">City</option>
                <option value="title">Title</option>
              </select>
              <div class="drop-down form-label" for="category">
                <i class="fa-solid fa-caret-down"></i>
              </div>
            <button class="btn btn-danger search-button ms-2" type="submit"><i
                class="fa-solid fa-magnifying-glass"></i></button>
          </form>
        </div>
      </div>
      <div class="navbar-nav ms-auto">
        <% if (currUser && currUser.role === "host") { %>
          <a class="nav-link" href="/listings/new">Add Listings</a>
        <% } %>
        <% if (!currUser) { %>
          <a class="nav-link" href="/login"><b>Login/Sign Up</b></a>
        <% } else{ %>
              <a class="nav-link" href="/logout">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>